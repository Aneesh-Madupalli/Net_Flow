# Binaries at repo root (ignored)
/*.exe
/*.exe~
/netflow
/netflow-*
*.dll
*.so
*.dylib

# Release folder: keep tracked so README download links work
# (Do not add release/ here. Explicitly allow these files in case of global *.exe etc.)
!release/
!release/netflow.exe
!release/netflow-macos-amd64
!release/netflow-macos-arm64
!release/netflow-linux-amd64

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/

